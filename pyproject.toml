[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecat"
version = "0.1.0b2"
description = "ElectroCatalysis Analysis Tools for electrochemical data handling and analysis."
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Luke Elissiry" },
]
license = { file = "LICENSE" }
dependencies = [
    "ipython",
    "jinja2",
    "matplotlib",
    "numpy",
    "pandas",
    "scikit-learn",
    "scipy",
]

classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Topic :: Scientific/Engineering :: Chemistry",
]

[project.optional-dependencies]
app = [
    "dash",
    "dash-ag-grid",
]
simulation = [
    "electrokitty",
]
simulation-all = [
    "electrokitty",
]

[project.scripts]
ecat-browser = "ecat_browser.app:main"

[tool.setuptools.packages.find]
where = ["src", "apps/browser/src"]

[tool.setuptools]
py-modules = ["eCAT"]

[tool.setuptools.package-data]
ecat = ["defaults.toml"]
ecat_browser = ["assets/*.css", "assets/*.svg"]
